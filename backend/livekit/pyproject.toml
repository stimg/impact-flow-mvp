[project]
name = "voice-agent"
version = "0.2.0"
requires-python = ">=3.11, <3.13.0"
dependencies = [
    "livekit-agents[elevenlabs,deepgram,silero,turn-detector]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.0.1",
]
